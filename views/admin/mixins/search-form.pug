mixin search-form(keyword)
    form(class="form input-group" id="form-search")
        input(type="text" class="form-control" placeholder="Nhập từ khóa" value=keyword form-search-input)
        button(type="submit" class="btn btn-primary") Tìm kiếm